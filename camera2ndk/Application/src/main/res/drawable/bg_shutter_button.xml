<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 外圈白色环 -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="4dp"
                android:color="@color/gc_shutter_outer" />
            <size
                android:width="72dp"
                android:height="72dp" />
        </shape>
    </item>
    <!-- 内部实心白色圆 -->
    <item
        android:bottom="6dp"
        android:left="6dp"
        android:right="6dp"
        android:top="6dp">
        <shape android:shape="oval">
            <solid android:color="@color/gc_shutter_inner" />
        </shape>
    </item>
</layer-list>
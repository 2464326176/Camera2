org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true

# AGP 7.4.2 / Gradle 7.5.1 require a JDK 11-17. This machine's default Gradle JVM is Java 8 and the
# Android Studio JBR is Java 25 (both incompatible), so point Gradle at the extracted Temurin JDK 17.
# JDK 21 triggers a JdkImageTransform (jlink) failure + StackOverflowError, so JDK 17 is required.
# Adjust this path if you move/reinstall the JDK.
org.gradle.java.home=E\:\\dev\\githubDesktop\\Camera2\\jdk17\\jdk-17.0.20+8
